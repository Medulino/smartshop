services:
  db:
    image: postgres:17
    container_name: compra_db
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build: .
    container_name: compra_web
    restart: always
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - static_files:/app/staticfiles
      - media_files:/app/media
      - logs_data:/app/logs
    # Límites de recursos para que un worker atascado no consuma el host
    mem_limit: 1g
    cpus: 2
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/salud/', timeout=3)\""]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  backup:
    image: postgres:17
    container_name: compra_backup
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      PGPASSWORD: ${DB_PASSWORD}
    volumes:
      - ./backups:/backups
    depends_on:
      db:
        condition: service_healthy
    entrypoint: >
      sh -c "while true; do
        umask 077;
        f=/backups/backup_$$(date +%Y%m%d_%H%M%S).sql;
        pg_dump -h db -U ${DB_USER} ${DB_NAME} > \"$$f\" && test -s \"$$f\" && echo \"Backup OK: $$f\" || { echo \"Backup FALLIDO: $$f\"; rm -f \"$$f\"; };
        find /backups -name 'backup_*.sql' -mtime +30 -delete;
        docker exec compra_web python manage.py limpiar_intentos || echo 'limpiar_intentos fallido';
        docker exec compra_web python manage.py clearsessions || echo 'clearsessions fallido';
        sleep 86400;
      done"

volumes:
  postgres_data:
  static_files:
  media_files:
  logs_data: